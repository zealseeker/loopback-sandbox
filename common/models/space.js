module.exports = function(Space) {

  // var app = require('../../server/server');
  // var Customer = app.models.customer;
  // Space.hasAndBelongsToMany(Customer);
  // Customer.hasAndBelongsToMany(Space);
};
